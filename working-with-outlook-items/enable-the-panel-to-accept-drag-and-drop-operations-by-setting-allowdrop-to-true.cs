using System;

class Program
{
    static void Main()
    {
        try
        {
            // This console sample does not include UI components.
            // Enabling drag-and-drop on a UI Panel is not applicable in a console application.
        }
        catch (Exception ex)
        {
            Console.Error.WriteLine(ex.Message);
        }
    }
}
