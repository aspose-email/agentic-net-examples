using System;
using Aspose.Email;

class Program
{
    static void Main()
    {
        try
        {
            // To use Aspose.Email in this project, add the NuGet package.
            // Run the following command in the terminal:
            // dotnet add package Aspose.Email
            Console.WriteLine("Execute the command above to install the Aspose.Email library.");
        }
        catch (Exception ex)
        {
            Console.Error.WriteLine(ex.Message);
        }
    }
}
