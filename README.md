# Agentic Aspose.Email .NET Examples

This repository is auto-managed. Key rules:
- Root docs (`README.md`, `agents.md`, `index.html`) are refreshed on each merge request.
- Category folders are lowercase with spaces replaced by hyphens (e.g., `programming-email-verification`).
- Each category folder includes `agent.md` with scope and naming reminders.
- Sample filenames are generated from the task name, must be meaningful, and **< 250 chars** including `.cs`.
- Use verified Aspose.Email APIs; validate inputs, wrap network/auth/file operations in try/catch, and avoid live calls when placeholders (e.g., `YOUR_`) are present.
